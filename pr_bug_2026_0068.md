## Summary
- reinitialize the Bootstrap notification dropdown after bell fragment refreshes
- hide/dispose the current dropdown before replacing menu HTML to prevent the bell from getting stuck after the first open
- add a regression test that locks in dropdown reinitialization logic

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`
