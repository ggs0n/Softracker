## Summary
- add regression coverage to lock payment routes behind authentication
- verify Stripe checkout success callback rejects sessions that do not belong to the current user
- prevent future direct-link authorization regressions in the payment module

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- current main already protects `PaymentController` with `[Authorize]` and validates Stripe `ClientReferenceId` ownership, so this PR keeps the production behavior unchanged and adds tests only
