## Summary
- normalize registration email before duplicate lookup and user creation
- reject duplicate logical email addresses caused by casing or whitespace differences
- add regression tests covering normalization and duplicate prevention

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this app uses email as the primary registration identity, so canonicalizing it in the service layer is the smallest safe fix
