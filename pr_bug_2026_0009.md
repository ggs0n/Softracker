## Summary
- normalize registration email before duplicate lookup and account creation
- reject duplicate logical email/username variants caused by casing or whitespace differences
- add regression tests covering normalization and duplicate prevention

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this app uses email as the username, so canonicalizing email also closes the username-casing duplicate path
