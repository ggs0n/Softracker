## Summary
- preserve project-scoped navigation for Create Feature cancel actions
- prefer a safe local returnUrl when provided
- fall back to `/ChangeRequest/Features?projectId=<id>` instead of the generic features list when returnUrl is missing
- add regression tests for cancel URL resolution

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this is a minimal navigation fix in the feature creation flow; no schema or behavior changes outside cancel-link routing
