## Summary
- stop persisting `SubscriptionPlan = Pro` before Stripe checkout succeeds
- allow checkout to start without a pre-saved Pro plan selection
- revert inactive pending Pro selections back to Free when checkout is canceled
- add regression tests for pending selection, cancel flow, and successful payment activation

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this is the smallest safe fix for partial success-state persistence on failed or canceled payment paths
