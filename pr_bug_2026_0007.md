## Summary
- add regression coverage for the already-hardened logout flow on `main`
- verify logout clears Identity sign-in state, session state, auth cookies, and anti-cache headers
- prevent future regressions in mixed Identity/JWT setups

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- current `main` already includes the runtime logout hardening; this PR locks it in with tests rather than changing working production behavior
