## Summary
- delete project-linked test cases before deleting a change request to prevent orphaned QA records
- keep the fix scoped to the actual delete path causing orphaned child rows
- add a regression test that locks in test-case cleanup before project deletion

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this is the smallest safe fix for orphaned child records without changing global EF delete behavior or adding broad cascade changes
