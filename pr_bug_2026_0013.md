## Summary
- add regression tests that lock `AdminController` behind `Authorize(Roles = "Admin")`
- verify all public admin actions remain covered by controller-level admin authorization
- prevent future accidental exposure of admin routes through refactors

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- current `main` already protects `AdminController` at the class level, so this PR hardens against regression instead of changing runtime behavior
