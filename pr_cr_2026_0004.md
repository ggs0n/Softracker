## Summary
- redesigned topbar clock from digital text to analog clock face
- kept existing date and timezone text for readability
- retained 1-second live updates and accessibility label updates

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- visual verification on authenticated dashboard: analog clock renders and updates each second

## Notes
- minimal-scope UI-only change in shared layout + site stylesheet; no backend behavior changes
