## Summary
- add `AsSplitQuery()` to the heaviest Change Request multi-include screens to avoid large joined result-set explosions as related data grows
- keep the controller/view behavior unchanged while reducing ORM query pressure on list/detail pages
- add a regression test that locks in split-query usage on the affected screens

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this is the smallest safe performance fix for ORM-backed list/detail screens without introducing repository refactors or broader query rewrites
