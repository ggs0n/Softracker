## Summary
- remove the hardcoded Pro price from the payment page
- display the same configured Stripe billing amount source that the checkout flow uses
- show a neutral Stripe-managed label when a Stripe Price ID is configured so the UI cannot drift from the actual charged amount
- add regression tests for both unit-amount and price-id billing configurations

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this is the smallest safe fix for payment amount mismatch without redesigning the checkout flow or introducing new pricing entities
