## Summary
- exclude inactive users from the admin dashboard team-member aggregation and employee listing
- prevent deactivated users from reappearing in active team counts and employee views
- add a regression test to lock in the `IsActive` filtering on both admin query paths

## Validation
- `dotnet build WFHMonitor/WFHMonitor.csproj -v minimal`
- `dotnet test WFHMonitor.Tests/WFHMonitor.Tests.csproj -v minimal`

## Notes
- this is the smallest safe fix for the reported inactive-record leakage without adding global query filters or changing unrelated modules
